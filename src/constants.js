export const USER_EXISTS_FEEDBACK = (userId) => {(
    `A user with the email ${userId} already exists`
)}

export const USER_NOT_FOUND_FEEDBACK = (userId) => {(`User with the ${userId} does not exist.`)}